# Core pipeline dependencies
# Phase 1: Crop-Inpaint-Stitch pipeline
opencv-python>=4.8.0
numpy>=1.24.0
pyyaml>=6.0
aiohttp>=3.8.0

# Phase 2: Temporal smoothing, Poisson blending
scikit-image>=0.21.0

# Phase 3B: Hyperparameter tuning
optuna>=3.0.0

# Optional deps managed via pyproject.toml extras:
# [streaming]: fastapi, pydantic, uvicorn
# [gpu]: torch, torchvision
# [dev]: pytest, pytest-asyncio
